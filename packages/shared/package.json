{
  "name": "@nbr/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, enums, DTO contracts, and workflow constants for the NBR Licensing Portal. Type-only — no runtime side effects.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "echo \"(shared) lint placeholder\"",
    "test": "echo \"(shared) test placeholder\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
